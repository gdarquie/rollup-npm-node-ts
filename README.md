# An example of a node npm package with rollup in typescript

## Install

```
cd aha && npm i && npm run build && cd .. && cd nest && npm i && cd ..
```

## Start

In dev, from nest:
```
npm run start:dev
```